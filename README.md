У меня есть окно, в котором есть 2 движущихся круга класса Particle.

Также я импортирую библиотеку gazeTrack: и создаю объект класса gazeTrack. Тут подробнее, у меня есть прибор - eyetracker, который считывает движение моих зрачков. При помощи gazeTrack.getGazeX(), gazeTrack.getGazeY() я могу получить координаты точки на экране куда я смотрю. 

Теория: я должна сравнить для каждого круга Parcticle его координат с координатами моего gazeTrack при помощи формулы, которую я получила от профа. 
Формула сравнивает координаты и выводит коэффициент. 
Мы решили с ним, что если коэффициент равен или выше 0.8, то объект считается followed и тогда должно происходить какое-то действие, например круг меняет цвет.

Этот проект сейчас это playground, потом когда все будет работать я создам графическую карту метро с анимированными поездами на ней, и когда кто-то следит взглядом за поездом, будет всплывать расписание этого маршрута.




У меня есть работающий проект с другой формулой подсчета коэффициентов (PearsonsCorrelation). Для этого я импортировала библиотеку apache.commons.math3.stat и метод сorrelation сравнивал два обычных массива. Кстати о массивах. Так как у меня данные о координатах идут непрерывным потоком, я использую ring buffer (class Queue), куда записываются при помощи метода add коодинаты.

Так вот к самой проблеме, для моего предыдущего варианта для формулы нужны были одномерные массивы. Новая же формула берет для кругов двумерные массивы m_PursuitX[j][i] и m_PursuitY[j][i], где j это сами круги (Particles у меня), а i это координаты. Т.е. мне нужно переделать мой мой ring buffer.


